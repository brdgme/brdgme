apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gameversions.brdgme.com
spec:
  group: brdgme.com
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            required: [typeName]
            properties:
              typeName:
                type: string
                description: Human-readable game type name (e.g. "Acquire").
              weight:
                type: number
                default: 0
                description: "Game complexity weight: 0 = light, 5 = heavy."
              isDeprecated:
                type: boolean
                default: false
                description: "Deprecated versions cannot start new games but remain running for in-progress games."
              scaleToZero:
                type: boolean
                default: false
                description: "Whether the game version's deployment should scale to zero replicas when idle."
          status:
            type: object
            properties:
              ready:
                type: boolean
              message:
                type: string
    subresources:
      status: {}
    additionalPrinterColumns:
    - name: Display Name
      type: string
      jsonPath: .spec.typeName
  scope: Namespaced
  names:
    plural: gameversions
    singular: gameversion
    kind: GameVersion
    shortNames:
    - gv
