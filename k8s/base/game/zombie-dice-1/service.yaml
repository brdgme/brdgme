---
apiVersion: v1
kind: Service
metadata:
  name: zombie-dice-1
  labels:
    app: zombie-dice-1
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: zombie-dice-1
