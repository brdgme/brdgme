---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lost-cities-2
spec:
  selector:
    matchLabels:
      app: lost-cities-2
  template:
    metadata:
      labels:
        app: lost-cities-2
    spec:
      containers:
      - name: lost-cities-2
        image: brdgme/lost-cities-2
        env:
        - name: ADDR
          value: "0.0.0.0:8080"
        ports:
        - containerPort: 8080
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 0
          periodSeconds: 1
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            memory: 64Mi
