// Game client module - placeholder for game process communication
// This will be expanded in later milestones

#[cfg(feature = "ssr")]
pub struct GameClient {
    // Placeholder for game process communication
}

#[cfg(feature = "ssr")]
impl GameClient {
    pub fn new() -> Self {
        Self {}
    }
}