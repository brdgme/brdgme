# 30: Friends - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.
>
> Extracted 2026-07-08 from `docs/plan/30-friends.md`. Task granularity is
> work-package level; run superpowers:writing-plans against the paired spec
> before execution if bite-sized steps are needed.

**Spec:** `docs/superpowers/specs/2026-07-08-30-friends-design.md`

## Tasks

- [ ] Prod data check: `SELECT count(*) FROM friends` (expected 0);
      if nonzero, review rows for pair duplicates before the
      uniqueness migration lands.
- [ ] Migration: per-direction UNIQUE + pair-unique index on
      `friends`; `users.invite_policy` with CHECK + default.
- [ ] db queries (`db.rs` or a new `friends` module): pair lookup,
      friend/pending lists, recent-co-player suggestions, friends'
      active games, friends' recent results, invite-policy check.
- [ ] Server fns: `send_friend_request` (by user id or exact name),
      `respond_to_friend_request`, `unfriend`, `list_friends`
      (accepted + pending both directions), `get_opponent_suggestions`,
      `set_invite_policy`.
- [ ] `create_new_game`: `opponent_ids` param wired through to the
      existing `CreateGameSeed.opponent_ids`; `check_invite_policy`
      called after email/id resolution (leave a pointer for #24's
      `create_proposal` to call the same helper).
- [ ] UI: `/friends` route + page (list, pending, add-by-name,
      invite-policy selector); add-friend on game-page player rows;
      dashboard sections (pending requests, friends' active games,
      friends' recent results); suggestion chips in the new-game form.
- [ ] #29 tie-in note: add-friend button on `/players/:name` when that
      phase lands (one line in 29-stats-reports.md).
- [ ] Tests (Phase 11 patterns): request/accept/decline/unfriend
      lifecycle; reverse-pending request auto-accepts; re-request
      after decline is a silent no-op; declined-side request flips to
      accepted; pair-uniqueness (A->B then B->A rejected at the db);
      unfriend then fresh re-request; invite policy open/friends/none
      incl. the add-by-email-of-existing-user path; self-request
      rejected; suggestions ordering/dedup/self-exclusion; dashboard
      queries against seeded fixtures.
