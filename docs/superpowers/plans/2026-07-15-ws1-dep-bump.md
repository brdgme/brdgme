# WS1: Full dependency bump - plan

Date: 2026-07-15
Spec: docs/superpowers/specs/2026-07-15-wasm-prod-errors-design.md (WS1)
Constraint: working-tree changes only; no commits, no pushes, no prod.

## Inventory findings (verified against registries 2026-07-15)

- Latest stable Rust: 1.97.0. rust-toolchain.toml is 1.96.1; Dockerfile chef
  image is rust-1.94.0 (already out of sync with each other).
- wasm-bindgen latest 0.2.126; pinned =0.2.121 in web/Cargo.toml, Dockerfile,
  ci.yml, and provided by devenv (nixpkgs). Lockstep move required; target
  version limited by what nixpkgs provides after `devenv update`.
- sqlx split-brain: web on 0.8 (locked 0.8.6), bot/operator on 0.9.0.
  Unify web to 0.9 if tower-sessions-sqlx-store supports it.
- Constraint bumps needed: gloo-net 0.6->0.7, tower-sessions 0.14->0.15,
  resend-rs 0.27->0.28, tokio-tungstenite 0.29->0.30 (dev-dep).
- web-sys/js-sys locked at 0.3.98 (latest 0.3.103), blocked by the
  wasm-bindgen =0.2.121 pin; unblocks with the wasm-bindgen bump.
- ~41 transitive packages move on plain `cargo update` (patch/minor).
- Dockerfile pins: cargo-chef 0.1.77 (current); image tag
  0.1.77-rust-1.97.0-bookworm exists on Docker Hub. cargo-leptos 0.3.6->0.3.7,
  wasm-bindgen-cli 0.2.121->lockstep target, dart-sass 1.97.3->1.101.0,
  sqlx-cli 0.8.6->0.9.0 (couple to web sqlx decision).
- ci.yml carries lockstep pins: wasm-bindgen-cli@0.2.121 (L183), dart-sass
  1.97.3 URL (L193), Playwright cache key 1.60.0 (L207).
- npm (end2end): @playwright/test 1.60.0->1.61.1, @types/node ^20->latest
  matching the node major in use, typescript ^5.4.5->7.0.2 (attempt; fall
  back if Playwright tooling breaks), @types/pg and pg already latest.
- devenv drift: sass 1.101.0 and sqlx-cli 0.9.0 already ahead of Dockerfile;
  cargo-leptos 0.3.6 and wasm-bindgen-cli 0.2.121 behind upstream.
  `devenv update` refreshes the nixpkgs pin (devenv.lock).
- Not present/not applicable: redis (not a dep anywhere). serde_yaml is
  deprecated upstream (bot) - note only, migration out of scope.

## Intended bumps

| Area | Item | From | To |
|------|------|------|----|
| toolchain | rust-toolchain.toml channel | 1.96.1 | 1.97.0 |
| Dockerfile | cargo-chef image | 0.1.77-rust-1.94.0-bookworm | 0.1.77-rust-1.97.0-bookworm |
| Dockerfile | cargo-leptos | 0.3.6 | 0.3.7 |
| Dockerfile+ci.yml+Cargo.toml | wasm-bindgen(-cli) | =0.2.121 | nixpkgs-provided target (0.2.126 if available) |
| Dockerfile+ci.yml | dart-sass | 1.97.3 | 1.101.0 |
| Dockerfile | sqlx-cli | 0.8.6 | 0.9.0 (if web sqlx moves to 0.9) |
| rust/web | sqlx | 0.8 | 0.9 (attempt; skip if store crate blocks) |
| rust/web | tower-sessions | 0.14.0 | 0.15 |
| rust/web | gloo-net | 0.6.0 | 0.7 |
| rust/web | resend-rs | 0.27 | 0.28 |
| rust/web | tokio-tungstenite (dev) | 0.29 | 0.30 |
| rust/web | web-sys/js-sys constraints | 0.3.77/0.3 | latest compatible |
| workspace | cargo update | - | all semver-compatible |
| devenv | devenv.lock (nixpkgs pin) | current | latest via `devenv update` |
| end2end | @playwright/test | 1.60.0 | 1.61.1 |
| end2end | @types/node | ^20.12.12 | match node major in use |
| end2end | typescript | ^5.4.5 | 7.0.2 (attempt) |

## Execution order

1. Parallel (disjoint files):
   - A: `devenv update`; report tool versions the new shell provides.
   - B: Rust workspace bumps except wasm-bindgen (manifest edits, cargo
     update, sqlx 0.9 migration attempt, build/test/clippy fixes).
   - E: end2end npm bumps (package.json + lockfile only; ci.yml untouched).
2. C (after A+B): wasm-bindgen lockstep bump in web/Cargo.toml to the
   version devenv provides; cargo update for the wasm-bindgen family.
3. D (after C+E): Dockerfile + ci.yml pin edits.
4. F: verification - cargo build + test + clippy for workspace, and
   `docker buildx bake web`.
5. G: refresh docs/CODING.md Dependency Management section (new
   wasm-bindgen pin, new pinned-by-ecosystem list, as-of date 2026-07-15).

## Skip rules

- Major bumps needing deep (non-mechanical) refactors are skipped and
  reported, constraint left as-is.
- Out of scope: brdgme-go/go.mod, renovate.json, websocket/ dir, wasm-opt
  and source-map work (WS2).
