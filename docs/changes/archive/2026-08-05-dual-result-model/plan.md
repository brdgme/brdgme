# Dual Result Model Plan

## Approach

Implement the approved dual-result model in dependency order. Keep all work
uncommitted until the complete result has independent review, user result
approval, and one final direct commit.

## Units

| ID | Objective | Dependencies | Verification | Status |
|---|---|---|---|---|
| DRM-01a | Add one fresh additive migration only: nullable checked end/departure metadata, approved unfinished-game legacy-departure backfill, and rollout-compatible constraints. | None | Migration/static SQL inspection; `git diff --check`. | Complete |
| DRM-01b | Add Rust models, row projections, and read/write query changes for nullable end/departure metadata, including old-pod rollout null tolerance. | DRM-01a | Focused static inspection; targeted web check if practical. | Complete |
| DRM-01c | Add focused schema/model tests, statically review SQL, run the permitted web check, and map DRM-01 acceptance evidence. | DRM-01a, DRM-01b | Focused test additions/static inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-02 | Implement pure competitive ranking and rating writer using `ranked_placing`. | DRM-01c | Focused pure/DB tests; targeted web check. | Complete |
| DRM-02a | Implement pure human competitive ranking from authoritative places and departure-event sequences. | DRM-01c | Focused pure tests; static inspection; targeted web check. | Complete |
| DRM-02b | Require complete human `ranked_placing` results for rating, then persist idempotent rating changes. | DRM-02a | Focused DB tests; static inspection; targeted web check. | Complete |
| DRM-03 | Update lifecycle writers and web/email actions for authorization, reasons, sequences, stale behavior, and notifications. | DRM-01c, DRM-02 | Focused tests/static inspection; targeted web check. | Complete |
| DRM-03a | Normalize unfinished legacy departures under the game-row lock and update service-status lifecycle writes: active elimination events, service completion places/end reason, competitive ranking, and ELO. | DRM-01c, DRM-02 | Focused writer tests/static transaction inspection; targeted web check. | Complete |
| DRM-03b | Update web and email concede/end actions and writers for shared departure events, bot replacement, concession forfeit, and current-state authorization/stale handling. | DRM-03a | Focused web/email action tests/static transaction inspection; targeted web check. | Complete |
| DRM-03b1 | Update locked concession and last-human-stop writers for normalized metadata, shared event sequences, forfeit/early-stop results, and locked stop authorization. | DRM-03a | Focused writer tests/static transaction inspection; targeted web check. | Complete |
| DRM-03b1a | Stamp replacement-concession departure metadata and a locked shared event sequence while retaining the human identity and replacing its bot. | DRM-03a | Focused writer tests/static transaction inspection; targeted web check. | Parked |
| DRM-03b1a1 | Change only the locked replacement-concession writer to normalize legacy rows, allocate the next event sequence, and persist the approved departure metadata. | DRM-03a | Static transaction inspection; targeted web check. | Parked |
| DRM-03b1a2 | Add focused replacement-concession metadata/continuation coverage after DRM-03b1a1. | DRM-03b1a1 | Focused writer tests/static inspection; targeted web check. | Blocked |
| DRM-03b1a3 | In `concede_game_replace` only, normalize legacy departures immediately after the locked claim; retain the already-left guard and bot selection; allocate the next positive sequence after successful bot selection; atomically retain the conceder's `user_id` while replacing `game_bot_id` and stamping `left_at`, `conceded`, and the sequence; leave the game active. Extend only `concede_game_replace_swaps_in_bot` with replacement-concession and unaffected-player metadata assertions. | DRM-03a | Static transaction inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b1b | Finalize two-human concession atomically with `concession_forfeit`, authoritative/competitive 1/2 places, and ELO. | DRM-03a | Focused writer tests/static transaction inspection; targeted web check. | Parked |
| DRM-03b1b1 | Persist the missing two-human concession end reason by writing `end_reason = 'concession_forfeit'` in the existing locked, stale-guarded, atomic game-row finalization only, and assert it in its existing rating test. | DRM-03a | Static transaction/diff inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b1b2 | In locked two-human `concede_game`, normalize legacy departures after the claim, allocate the next positive departure sequence after the two-player guard, and stamp only the conceder's departure metadata; extend the existing rating test. | DRM-03b1b1 | Static transaction/diff inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b1c | Enforce locked last-human-stop authorization and atomically persist its no-game-place competitive result, cleanup, and end reason. | DRM-03a | Focused writer tests/static transaction inspection; targeted web check. | Parked |
| DRM-03b1c1 | Change only the last-human-stop writer and mechanical caller actor forwarding for locked authorization, no-place finalization, and competitive ranking/rating. | DRM-03a | Static transaction inspection; targeted web check. | Parked |
| DRM-03b1c2 | Add focused last-human-stop authorization and result coverage after DRM-03b1c4. | DRM-03b1c4 | Focused writer tests/static inspection; targeted web check. | Complete |
| DRM-03b1c3 | In `end_game` only, normalize legacy departures after the locked claim; atomically persist `last_human_stop` with the existing stale guard; clear all authoritative places and terminal seat state; retain competitive ranking/rating; extend only `end_game_finishes_and_ranks`. | DRM-03a | Static transaction inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b1c4 | Update the locked last-human-stop authorization and necessary mechanical caller actor forwarding after DRM-03b1c3, without changing finalization semantics. | DRM-03b1c3 | Static transaction/caller inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b2 | Align web/email concede/end action prechecks and capability state with the locked writer authorization and stale behavior. | DRM-03b2a, DRM-03b2a1, DRM-03b2b, DRM-03b2c | Aggregate child evidence. | Complete |
| DRM-03b2a | Introduce a shared web/email `end_core` that preserves the pre-write `GameExtended` notification snapshot, delegates both End actions to the locked writer with the actor game-player ID, classifies finished/stale writer errors as conflicts, and rejects concede whenever fewer than two active humans remain before replacement/forfeit dispatch. | DRM-03b1a3, DRM-03b1b2, DRM-03b1c2 | Lead source/caller/writer/test inspection; independent authorization review; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b2a1 | Revalidate the approved two-active-human Concede threshold inside both locked concede writers after legacy normalization and before lifecycle mutation; return a typed conflict and prove rejected calls make no lifecycle change. | DRM-03b2a | Lead source/writer/test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03b2b | Align web capability flags with the approved two-active-human Concede threshold and End availability. | DRM-03b2a1 | Focused capability inspection/tests; targeted web check; `git diff --check`. | Complete |
| DRM-03b2c | Add only action-parity tests in the existing web and email command test modules: shared valid/invalid End authorization and typed conflict outcomes, plus sole-active Concede rejection with End applicability. Reuse the b2b capability matrix and c2 writer coverage; do not change production behavior, writers, wording, notifications, or capability logic. | DRM-03b2b | Focused test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03c | Align lifecycle notifications, early-stop wording, undo departure reversal, and focused lifecycle regression coverage. | DRM-03c1, DRM-03c2a, DRM-03c2b | Aggregate child evidence. | Complete |
| DRM-03c1 | In `undo_game`, when existing undo semantics reverse an active departure/unelimination and clear `left_at`, clear `departure_reason` and `departure_sequence` in the same row update. Extend only `undo_game_clears_left_at_on_unelimination_and_restores_points` to prove the eliminated player starts with `left_at`, `eliminated`, and a positive sequence, then all three departure fields are null after undo while retaining existing points assertions. | DRM-03b | Lead hunk and completed-history protection inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-03c2 | Align Finished lifecycle notification wording and focused coverage. | DRM-03c2a, DRM-03c2b | Aggregate child evidence. | Complete |
| DRM-03c2a | In `rust/web/src/email/notify.rs` only, add the `game.end_reason == Some("last_human_stop")` Finished branch with exact header `Game ended early. No game result.`. That branch must not call winner/placing-header logic, name a winner, or present rating deltas as a game result. Keep every other Finished path byte/behavior equivalent. Add exactly one direct focused `build_content` test proving the exact header and absence of winner/result language derived from place, points, ranked placing, and rating delta. | DRM-03b | Lead source/test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB/email execution remains CI/capable-machine-only. | Complete |
| DRM-03c2b | In `rust/web/src/email/notify.rs` only, add the deferred focused normal game-service finish, two-human concession, and replacement-concession notification coverage. Preserve the accepted c2a no-result branch and all Finished behavior. | DRM-03c2a | Lead source/test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB/email execution remains CI/capable-machine-only. | Complete |
| DRM-04 | Upgrade export/import to schema v2 with replacement/result metadata. | DRM-04a, DRM-04b, DRM-04c | Aggregate child evidence. | Complete |
| DRM-04a | In `game/export.rs`, add nullable game `end_reason`; required player `is_human`; nullable `ranked_placing`, `departure_reason`, positive `departure_sequence`, and `left_at`; and exact player `created_at`, `updated_at`, `is_turn_at`, and `last_turn_at`. Map directly from `GameExtended` projections; `is_human` is true when `user_id` exists; retain `bot_name` for pure bots and replacement-human seats. Keep `BUNDLE_SCHEMA_VERSION` at 1. Update only compile-required `BundleGame`/`BundlePlayer` literals in `game/import.rs`; no import behavior/assertion change. | DRM-01c | Lead field/projection and replacement-identity inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Complete |
| DRM-04b | Update only import production writes and focused import evidence to consume every DRM-04a required field directly, preserving all player timestamps exactly and retaining replacement-human identity. No defaults, compatibility path, schema-version change, or envelope-rejection change. | DRM-04a | Lead import mapping, identity, transaction, trigger, and test-boundary inspection; targeted web check; `git diff --check`. | Complete |
| DRM-04c | Set `BUNDLE_SCHEMA_VERSION` to exactly 2. CLI/file JSON import must deserialize a minimal version envelope and reject v1 or any non-2 version before full v2 deserialization, with no defaults for missing v2 fields. Retain `import_bundle` exact-version validation before writes. Add focused round-trip/import evidence for end/result/departure metadata, every player timestamp, all approved identity cases, v1 no-write rejection, future-version rejection, and schema-version assertions including SSR coverage. | DRM-04b | Lead version-envelope ordering and complete evidence-matrix inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB/SSR runtime tests remain CI-only. | Complete |
| DRM-05 | Update competitive queries/stats/markers and Game results UI while preserving SSR structure. | DRM-05a, DRM-05b, DRM-05c, DRM-05d, DRM-05e | Aggregate child evidence. | Complete |
| DRM-05a | In `rust/web/src/stats/queries.rs` only, make `overall_totals`, `game_type_stats`, `head_to_head`, `recent_form`, and `recent_form_for_game_type` competitive-only: count eligible human rows with non-null `ranked_placing`; exclude pure bots and historical null-ranked rows; require competitive ranks for both head-to-head humans; remove every `COALESCE(ranked_placing, place)` fallback; and filter null ranks inside each form qualifying CTE before `row_number()`. Preserve rating-series and unrelated history/game-result queries. Add focused same-module DB coverage for historical-null exclusion, pure-bot exclusion, replacement-human inclusion, competitive ties/denominators, head-to-head, and form ordering/window filtering. | DRM-01c, DRM-02, DRM-03 | Lead SQL/test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB tests CI-only. | Accepted |
| DRM-05b | Update only the main-profile data/server boundary to remove the legacy `?bots=1` inclusion contract and expose the approved competitive versus authoritative result selection without changing the existing history route. | DRM-05b1, DRM-05b2, DRM-05b3 | Aggregate child evidence. | Complete |
| DRM-05b1 | In `rust/web/src/stats/queries.rs` only, make `FinishedGameRow` and `finished_games` use competitive placement exclusively from non-null `ranked_placing`. Count only eligible ranked human rows for participant counts and denominators, retaining replacement humans while excluding pure bots and historical null-ranked rows. Never select or fall back to authoritative `place`. Add focused same-module DB tests for ranked humans, replacement-human inclusion, pure-bot exclusion, historical null-rank exclusion, ties, and ranked-human player count. | DRM-05a | Lead query/DTO/test hunk inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB tests CI-only. | Accepted |
| DRM-05b2 | Rename the competitive `FinishedGameRow` field to `ranked_placing` at its DTO/query mapping boundary, then update only main-profile and game-type deep-dive recent placing, marker, and histogram consumers to use it. Missing rank yields no marker/result; authoritative `place` and the history route/query are untouched. Preserve the existing resource key, `Suspense`, and layout structure. | DRM-05b1 | Lead inspected the DTO/query mapping, both page consumers, histogram/formatting tests, and hydration structure; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. | Accepted |
| DRM-05b3 | Add deferred main-profile SSR coverage for the competitive boundary after consumer/histogram alignment. | DRM-05b2 | Focused SSR coverage and hydration-structure inspection; targeted web check; `git diff --check`. | Accepted |
| DRM-05c | In `rust/web/src/stats/queries.rs` only, add a distinct authoritative Game results DTO/query and focused DB coverage. Select completed games in which the profile human participated; return every seat, including pure bots and replacement-human seats with both human and replacement-bot identity, ordered by game recency then seat position. Expose nullable authoritative `place` and `end_reason` only: no `ranked_placing` or points result/fallback. A `last_human_stop` remains completed with every seat place null. Keep this separate from `FinishedGameRow` and existing history. | DRM-05b3 | Lead DTO/query/identity/test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB tests CI-only. | Accepted |
| DRM-05d | Add the main-profile server/resource/UI boundary for the authoritative Game results DTO/query while preserving existing SSR/hydration structure. | DRM-05d1, DRM-05d2, DRM-05d3 | Aggregate child evidence. | Complete |
| DRM-05d1 | Move `GameResult` and `GameResultSeat` to client-visible `stats/mod.rs`, then add the minimal `PlayerResultsData` and `get_player_results(name)` server boundary. Reuse every existing profile-header datum and authoritative `game_results`; preserve the sibling `Ok(None)` unknown-player convention. Add focused server-entry unknown-player coverage only. | DRM-05c | Lead visibility/serde/server-boundary/unchanged-05c-SQL inspection; focused server-entry test inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. DB execution CI-only. | Accepted |
| DRM-05d2 | On the main profile only, parse `view=results` with Competitive as the missing/unknown default; ignore main-profile `bots=1`; make the selected view an unconditional input to the existing single blocking resource; dispatch competitive profile or authoritative results data through one client-visible response shape; and render the approved Competitive/Game results selector and authoritative results branch. Render each authoritative seat's `Game placing` label and value only when its `place` is `Some`; null early-stop seats render identity only. Preserve the existing blocking-resource, `MainLayout`, and sole `Suspense` hydration structure. | DRM-05d1 | Lead URL/default/resource/rendering/hydration inspection; `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`; `git diff --check`. SSR/hydration runtime CI-only. | Complete |
| DRM-05d3 | Add only the focused SSR presentation coverage required for the approved main-profile Game results view. | DRM-05d2 | Lead SSR/hydration inspection; targeted web check; `git diff --check`. SSR runtime CI-only. | Complete |
| DRM-05e | Update main-profile competitive presentation and recent markers to use competitive results only, with no marker for null `ranked_placing`; preserve authoritative result presentation outside the approved Game results surface. | DRM-05a, DRM-05d | Focused presentation/marker inspection/tests; targeted web check; `git diff --check`. | Complete |
| DRM-R1 | Independently review all implementation units and evidence; dispatch bounded fixes and re-review if required. | DRM-01a..DRM-05 | Concrete evidence-backed findings and final diff inspection. | Complete |

## Evidence Map

| AC | Evidence target |
|---|---|
| 1 | Migration-032 static inspection; `fresh_game_and_players_read_null_metadata`, `checked_end_and_departure_metadata_round_trips`, and `migration_checks_reject_invalid_end_and_departure_metadata`; targeted web check passed. DB tests and migration execution require CI/capable machine. |
| 2 | Migration-032 static inspection verifies unfinished human `left_at` filtering, per-game `dense_rank()` ties, and only departure metadata writes. Seeded backfill execution requires CI/capable machine. |
| 3-8 | DRM-02a pure tests cover active/departed order, bot filtering, replacement humans, authoritative and departure-event ties, reverse event sequence, all-active normal finishes, last-human one-/zero-active results, incomplete ranks, and fewer than two humans. DRM-02b DB tests cover rank-only eligibility, no `place` fallback, pure-bot exclusion, replacement humans, zero deltas, and retry idempotency. DRM-03b1b1 makes the existing locked, stale-guarded two-human concession game update persist `end_reason = 'concession_forfeit'`; its existing rating test asserts that durable reason. DRM-03b1b2 normalizes legacy departures under that existing lock, allocates a positive sequence after the two-player guard, and persists `left_at`, `conceded`, and that sequence only for the conceder; the same test asserts complete conceder metadata and absent winner metadata. DRM-03b1a3 applies the corresponding locked normalization, post-bot-selection sequence allocation, atomic replacement-concession metadata, and focused replacement/unaffected-player assertions, including exact selected replacement-bot identity. DRM-03b1c3 normalizes after the locked claim, stale-guards `last_human_stop` finalization, clears authoritative places and terminal seat state, then retains ranking/rating; its existing test asserts end reason, all-seat cleanup, human competitive 1/2, and pure-bot absence. DRM-03b1c4 retains the claim-normalization-authorization-finalization order, passes `game_players.id` from web, email, and direct callers, and limits zero-active latest-departure authorization to human rows; its existing trigger fixture now supplies the authorized one-active-human state without changing trigger assertions. DRM-03b1c2 adds seven isolated DB tests for sole-active success, departed rejection, latest-event success, tied-latest authorization on separate unfinished games, earlier-event rejection, pure-bot rejection, and multi-active rejection. Every rejected case asserts unfinished/null end state, seeded authoritative places, null result/rating fields, unchanged seeded ratings, state, and no end log. Lead inspected the actual test hunk and confirmed `end_game_finishes_and_ranks` remains unchanged. The required targeted web check and `git diff --check` pass. DB test execution remains CI/capable-machine evidence. |
| 9-10 | DRM-05a accepted after one fresh Worker corrected only the focused `queries.rs` tests. `game_type_stats_computes_avg_place_percentile` now expects its actual one win from two eligible games. Each newest null-ranked form fixture now has two independently eligible ranked human opponents, so the target user's null `ranked_placing` is the sole reason it is excluded before `row_number()`; both valid-ranked non-displacement assertions remain. Lead inspected all five production queries and the corrected test hunks. DRM-05b1 accepted after a fresh Worker replaced its changed `finished_games` macro query with plain `query_as`/`FromRow`, retaining every query predicate, order, and bind while avoiding offline macro metadata. Lead inspected the full query, local row decoder, `FinishedGameRow` mapping, and five focused DB tests for ranked placement, ranked-human counts, historical null ranks, replacement humans, pure bots, and ties. DRM-05b2 makes that public DTO explicit as `ranked_placing`; both main-profile/game-type placing consumers and histograms read it, and pure tests cover rank-only values, ties, and missing-rank omission. DRM-05b3's profile and deep-dive SSR fixtures seed explicit competitive ranks; the deep-dive fixture now gives the page subject authoritative place 1 and competitive rank 2, then asserts `2nd of 2` renders and `1st of 2` does not. DRM-05c adds separate `GameResult`/`GameResultSeat` data via plain SQLx decoding: the query filters completed participant games, orders finished recency/game ID/seat position, returns every seat's human and bot identities, and selects only nullable authoritative `place` plus `end_reason`. Focused DB tests cover normal and concession finishes, pure bots, replacement human plus bot identity, finished-only filtering, deterministic order, and no-place last-human stops. Resource keys, `Suspense`, layout, history, Game results UI, and rating series are unchanged. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass; DB and SSR runtime execution remain CI-only. The required DRM-05a/05b1/05b2 source and backlog #59 entries are recorded in `/home/beefsack/Development/brdgme-commit-plan.md`. |
| 11 | DRM-04a direct export/projection inspection establishes required v2 fields, exact timestamps, and replacement identity; DRM-04b import writes establish direct persistence without defaults or inference; DRM-04c inspection/tests establish envelope-first v1/non-2 rejection before full deserialization or DB writes, exact version 2 export/import, complete result/departure/timestamp round-trip preservation, human-only/pure-bot/replacement-human identity preservation, future-version rejection, and SSR export version coverage. |
| 12 | DRM-03a accepted: static lock/transaction inspection; eight focused CI-only writer tests for normalization, elimination, service completion, and terminal non-inference; targeted web check passed. After two reportless broad DRM-03b1 attempts, approved recovery splits the locked writers into DRM-03b1a replacement metadata, DRM-03b1b two-human forfeit, and DRM-03b1c last-human stop. A reportless DRM-03b1a attempt further narrows to writer-only DRM-03b1a1 then focused coverage DRM-03b1a2, followed by web/email authorization, notification, and undo parity evidence. |
| 13 | Hydration-structure inspection and available targeted SSR/CI evidence. |

### DRM-03b2a Evidence

- Shared behavior: `end_core` in `rust/web/src/game/server_fns.rs` loads and returns the pre-write `GameExtended`, passes its `updated_at` and resolved `game_players.id` to the locked `db::end_game` writer, and maps writer errors through `conflict_or_internal`. Both web `end_game` and email `run_end` call it and notify with `Some(before)`.
- Concede threshold: `concede_core` rejects fewer than two active humans before replacement-bot lookup and both replacement/forfeit dispatches. `concede_core_rejects_sole_active_human_before_dispatch` asserts the game remains unfinished and the human is not replaced.
- Focused coverage: `conflict_or_internal_maps_typed_finished_and_stale_errors`, `end_core_rejects_finished_game`, `end_core_rejects_when_two_active_humans`, `end_core_ends_solo_human_and_returns_pre_write_snapshot`, and email `run_end_ends_game_for_last_human`. DB-backed tests require CI/capable-machine execution.
- Verification: Lead and independent authorization review inspected the shared callers, notification snapshot use, locked writer interface/authorization, and tests. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed; `git diff --check` passed.

### DRM-03b2a1 Evidence

- Accepted: Lead inspection confirms both writers claim and normalize under the game-row lock, then return `NotEnoughActiveHumans` from the active-human count before any lifecycle write. `conflict_or_internal` reuses the existing b2a conflict message. The focused rejection tests preserve the unfinished game, conceder, places, ranks, ratings, logs, replacement state, and the seeded departed human's exact `left_at`, `departure_reason`, and `departure_sequence` after rejection.
- Verification: `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass. DB-backed execution remains CI/capable-machine-only.

### DRM-03b2b Evidence

- Shared predicates: `concede_actor_eligible` and `concede_eligible` establish active-human, two-active-human, replacement, and two-seat-forfeit eligibility; `end_eligible` establishes sole-active or zero-active latest-human-departure-tie eligibility. `concede_core`, `end_core`, and `GameViewData` capability fields use those predicates. Locked writers remain authoritative for races.
- Focused pure matrix: seven tests cover replacement/no-replacement and two-seat-forfeit Concede cases; one-active End-over-Concede precedence; zero-active latest-event ties and earlier departures; pure-bot exclusion; multi-active End rejection; and invariance to `place`, `ranked_placing`, `points`, and timestamp values.
- Verification: Lead inspected the predicates, all core/capability call sites, and the matrix. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass. The compile check repeats unrelated existing warnings for unused `NotifyKind::Reminder`, unnecessary `mut` in `bin/import_game.rs`, and transitive `proc-macro-error2` future incompatibility.

### DRM-03b2c Evidence

- Web/email End parity: existing sole-active tests cover `end_core` and `run_end`; added tests cover departed-actor and earlier-event rejection on both surfaces, web/email multi-active rejection, and zero-active tied-latest-event success through one tied participant per surface. The c2 writer test retains the per-tied-participant loop and is not duplicated.
- Conflict and Concede outcomes: `conflict_or_internal_maps_typed_finished_and_stale_errors` establishes typed writer errors' shared messages; email classification returns those exact strings as user errors, and `run_end_rejects_finished_game` covers the finished action path. `run_concede_rejects_sole_active_human_then_end_applies` rejects Concede without replacement, then ends the same game; b2b's pure capability matrix remains unchanged.
- Verification: Lead inspected the actual test hunks and coverage boundaries. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with only existing `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. DB/runtime execution remains CI-only.

### DRM-03c1 Evidence

- Undo writer: the existing unelimination branch in `undo_game` clears `departure_reason` and `departure_sequence` in the same row update that clears `left_at`. Undo direction detection, points restoration, transaction/stale behavior, finished-game rejection, ratings, and all other lifecycle behavior are unchanged.
- Completed-history protection: `claim_unfinished_game_tx` rejects finished games before the game-player update, and the existing game update retains `is_finished = false`; the changed branch does not broaden either boundary.
- Focused coverage: `undo_game_clears_left_at_on_unelimination_and_restores_points` proves the eliminated player has `left_at`, `eliminated`, and a positive sequence before undo, then proves all three departure fields are null after undo while retaining its original points assertion.
- Verification: Lead inspected the writer, focused test, and completed-history boundary. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` passed. DB execution remains CI-only.

### DRM-03c2a Evidence

- Finished handling checks `game.end_reason.as_deref() == Some("last_human_stop")` before any player collection, place sorting, winner construction, or rating-delta presentation, returning exactly `Game ended early. No game result.`. Its non-early-stop fallback preserves the prior winner/placing-header logic.
- The sole direct `build_content` test seeds distinct non-null place, points, ranked placing, and rating deltas for both human players on a `last_human_stop` game, then asserts the exact header, no winner label, and no player name.
- Verification: Lead inspected the actual source/test diff. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with existing `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. The DB-backed focused test remains CI/capable-machine-only.

### DRM-03c2b Evidence

- `finished_game_service_header_uses_authoritative_place` gives position 1 authoritative place 1 while position 0 has conflicting higher points and ranked place 1, then asserts the exact Finished header keeps position 1 first. `finished_two_human_concession_header_uses_authoritative_places` similarly asserts authoritative 1/2 for the concession despite conflicting points and ranked places.
- `replacement_concession_emits_no_finished_or_eliminated_notification` starts from a pre-concession snapshot, leaves the game unfinished, retains the conceding human while assigning its replacement bot and conceded departure metadata, then asserts neither human receives an email token. The routing path can mint a token only after selecting a notification recipient, establishing no Finished or Eliminated send.
- Verification: Lead inspected all three exact test hunks and the Finished/routing paths. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with existing `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. DB/email runtime tests remain CI/capable-machine-only.

### DRM-04a Evidence

- `BundleGame.end_reason` maps directly from `ge.game.end_reason`. `BundlePlayer` maps `ranked_placing`, `departure_reason`, `departure_sequence`, `left_at`, `created_at`, `updated_at`, `is_turn_at`, and `last_turn_at` directly from `p.game_player`.
- `is_human` is `p.user.is_some()`, the human `user_id` projection. `bot_name` remains independently mapped from `p.game_bot.name`, so a replacement-human seat retains both human identity and its replacement bot seat name; pure bots have `is_human = false`.
- `BUNDLE_SCHEMA_VERSION` remains 1. `import.rs` has no direct `BundleGame` or `BundlePlayer` literals because its fixtures use `build_export_bundle`, so no fixture edit was required. Import production behavior and assertions are unchanged.
- Verification: Lead inspected the exported structs, mapping, and `GameExtended`/`GamePlayer` projections. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass. The check repeats the known `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings.

### DRM-04b Evidence

- Direct import writes preserve `games.end_reason`; player authoritative and competitive places, departure metadata, and every exported player timestamp (`created_at`, `updated_at`, `is_turn_at`, `last_turn_at`) without defaults or inference.
- Identity matching imports human-only seats as users, pure bots as bot-only seats, and replacement humans as both a user and a bot. A non-human seat without a bot name and unknown bot references return from the open transaction before commit.
- The existing transaction still commits only after player and log imports. `games` and `game_players` timestamp triggers are `BEFORE UPDATE`, so supplied INSERT timestamps remain exact. The existing turn-timestamp test is restored to its pre-unit assertions; no unapproved timestamp-test expansion remains.
- Verification: Lead inspected the complete `import.rs` diff and the export fields. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with only the known `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. DB execution remains CI/capable-machine-only.

### DRM-04c Evidence

- `BUNDLE_SCHEMA_VERSION` is exactly 2. The CLI `parse_bundle` first deserializes only `VersionEnvelope { schema_version }`, rejects every non-2 version with an explicit version error, then deserializes the full `ExportBundle`. The v2 structs retain required fields without serde defaults, so missing v2 fields fail full deserialization rather than being inferred.
- `parse_bundle_rejects_v1_envelope_before_full_decode` and `parse_bundle_rejects_future_version_before_full_decode` use version-only JSON and assert explicit v1/999 errors, proving the envelope gate precedes full v2 deserialization. `parse_bundle_accepts_current_version` proves a serialized v2 bundle succeeds. `import_bundle_rejects_v1_envelope_without_writes` and strengthened `import_bundle_rejects_unknown_schema_version` assert explicit errors and unchanged game counts. `import_bundle` retains its exact equality gate before `pool.begin()`.
- `import_bundle_round_trips_result_and_departure_metadata` asserts exact game end reason/finish time and per-player authoritative place, competitive place, departure reason, sequence, and nullable `left_at`. `import_bundle_round_trips_player_timestamps_exactly` uses distinct values to assert exact `created_at`, `updated_at`, `is_turn_at`, `last_turn_at`, and nullable `left_at` preservation. Dedicated tests establish human-only, pure-bot, and replacement-human-plus-bot identity. SSR export-route coverage asserts schema version 2.
- Verification: Lead inspected the actual envelope order, importer pre-write gate, struct fields, complete test matrix, and SSR assertion. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with existing `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. DB and SSR runtime test execution remains CI-only.

### DRM-05d1 Evidence

- `GameResult` and `GameResultSeat` are public, client-visible `stats/mod.rs` DTOs with `Debug`, `Clone`, `PartialEq`, `Serialize`, and `Deserialize`; `PlayerResultsData` exposes those authoritative results plus the existing profile-header `viewer_user_id` and `can_add_friend` data.
- `get_player_results` exactly mirrors `get_player_profile` header semantics: authenticated viewer ID, false for anonymous/self viewers, and `!should_hide_add_friend` with the `get_player_results: friend status` error context for another viewer. Its existing unknown-player server-entry test retains sibling `Ok(None)` behavior before result work.
- Lead inspected the complete d1 diff, `players.rs` header consumer, and unchanged DRM-05c authoritative SQL and focused DB tests. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass. DB-backed server-entry execution remains CI/capable-machine-only.

### DRM-05d2 Evidence

- `PlayersPage` parses only `view=results`; missing and unknown values select Competitive, and main-profile `bots=1` is absent from the resource key, dispatch, selector, and game-type links. Game-type and history resource keys remain unchanged.
- The unconditional sole `Resource::new_blocking` key is `(name, view)`. It dispatches Competitive to `get_player_profile(name, false)` and Game results to `get_player_results(name)`, serializing the client-visible `PlayerPageData` enum. Its sole unconditional `.get()` remains first in the existing sole `Suspense` closure, with `MainLayout` outside that boundary.
- Both branches reuse the profile header/Add friend behavior. The Game results branch renders every query-returned ordered seat, preserving pure-bot and replacement human-plus-bot identity, reads only nullable authoritative `place`, labels its column `Game placing`, and emits no placement value for null-place early stops.
- DRM-05d2 correction: each seat identity remains unconditional; only `Some(place)` renders the adjacent `Game placing` label and authoritative value. Null early-stop seats render neither label nor value. The correction changes no resource, boundary, or layout structure.
- Lead inspected the actual resource key/read/layout, both branches, `GameResult` query shape/order, the per-seat conditional, and existing game-type/history resource paths. `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` and `git diff --check` pass; runtime SSR/hydration remains CI-only.

### DRM-05d3 Evidence

- Focused SSR presentation fixtures retain Competitive missing/unknown-view defaults, ignored main-profile `bots=1`, all ordered authoritative seat identities including pure-bot and replacement-human-plus-bot seats, and authoritative values distinct from competitive ranks.
- The completed `last_human_stop` fixture retains both null-place seat identities and, with the accepted d2 per-seat rendering correction, emits neither `Game placing` nor a placement value for either seat.
- Lead inspected the complete d2 rendering and d3 test hunks. The sole blocking resource remains unconditionally keyed by `(name, view)`, its sole `.get()` is first in the sole `Suspense` closure, and `MainLayout` remains outside. The new conditional derives only from blocking-resource data, so SSR and initial hydration construct identical markup and no resource/boundary IDs change.
- Verification: `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr` passed with existing `NotifyKind::Reminder`, `import_game.rs` unnecessary-`mut`, and transitive `proc-macro-error2` warnings; `git diff --check` passed. SSR/hydration runtime execution remains CI-only.

## Constraints

- Determine the migration number from the filesystem immediately before creating it; add exactly one migration and never edit an existing migration.
- DRM-01a owns only migration SQL; DRM-01b owns Rust production model/projection/query edits; DRM-01c owns focused tests and DRM-01 acceptance evidence.
- No game-service protocol, gameplay/rules, points, timer/timeout implementation, retrospective repair, deployment manifest, or old migration work.
- Local Rust verification is limited to `SQLX_OFFLINE=true cargo check -p web --all-targets --features ssr`, `git diff --check`, and static inspection. No prohibited Cargo, database, SQLx, formatting, clippy, SSR runtime, or hydration runtime command.
- Preserve the approved SSR/hydration resource structure.
- DRM-04a must not change `BUNDLE_SCHEMA_VERSION`, import production behavior,
  import assertions, schema, or migrations. It may update only `BundleGame` and
  `BundlePlayer` import-test fixture literals required to compile. DRM-04b
  preserves every player timestamp exactly. DRM-04c alone owns the atomic
  version flip and explicit v1-envelope rejection.
- DRM-05a is restricted to `rust/web/src/stats/queries.rs`; it does not change
  UI, models, server functions, authoritative history/game-result queries,
  migrations, or schema. DB-backed tests are CI-only. DRM-05b..e keep the
  selector on the main profile only; `?bots=1` is removed and ignored, the
  history route is unchanged, and broader game-type/history rollout is #59.

## Residual Risks

- Migration execution, database tests, SSR runtime, hydration runtime, formatting, and clippy require CI/capable-machine evidence under the resource constraints.

## Approval

- Final outcome: all approved units and acceptance criteria are complete; residual CI/capable-machine verification limits are recorded above. User approved the completed result and archival transaction on 2026-08-05.
- User approved this initial plan and the Orchestrator artifact gate.
- Orchestrator approved the 2026-08-05 administrative addition of DRM-03b1b1 beneath parked DRM-03b1b. It preserves approved semantics and isolates persistence of the missing two-human concession end reason.
- Orchestrator approved the 2026-08-05 administrative addition of DRM-03b1b2 beneath parked DRM-03b1b. It preserves approved semantics and isolates locked conceder departure metadata.
- Orchestrator approved the 2026-08-05 administrative addition of DRM-03b1a3 beneath parked DRM-03b1a. It preserves approved semantics and isolates replacement-concession recovery in the existing locked writer and test.
- Orchestrator approved the 2026-08-05 administrative addition of DRM-03b1c3 and follow-on DRM-03b1c4 beneath parked DRM-03b1c. DRM-03b1c3 isolates last-human-stop finalization; DRM-03b1c4 retains the deferred authorization/caller work. DRM-03b1c2 now depends on DRM-03b1c4. The amendment preserves approved semantics and acceptance criteria.
- Orchestrator approved the 2026-08-05 c4 corrections: zero-active authorization computes its latest departure event from human participant rows only, and the existing trigger fixture may establish an authorized one-active-human stop without new authorization coverage. The corrections preserve approved semantics and acceptance criteria.
- User and Orchestrator approved the 2026-08-05 DRM-03b2 split into DRM-03b2a, DRM-03b2b, and DRM-03b2c. DRM-03b2a also enforces the approved two-active-human Concede threshold in shared behavior; DRM-03b2b owns capability flags; DRM-03b2c owns the remaining action and notification matrix. The split preserves the approved lifecycle outcome and acceptance criteria.
- Orchestrator approved the 2026-08-05 administrative addition of DRM-03b2a1 after DRM-03b2a, with DRM-03b2b depending on it. The child closes the approved threshold's locked-writer concurrency gap without changing lifecycle semantics or acceptance criteria.
- Orchestrator approved the 2026-08-05 split of DRM-03c into DRM-03c1 undo departure reversal and DRM-03c2 lifecycle notification wording. DRM-03c1 is current; DRM-03c2 is parked pending a product notification decision. The split preserves approved lifecycle semantics and acceptance criteria.
- User and Orchestrator approved the 2026-08-05 DRM-03c2 notification decision and this plan amendment. The child is unblocked without changing the approved lifecycle model: replacement concession is notification-free; `last_human_stop` has the exact no-result header; other Finished notifications use authoritative game places only.
- Orchestrator approved the 2026-08-05 administrative recovery split of reportless DRM-03c2 into serial DRM-03c2a and DRM-03c2b. DRM-03c2a is current for only the last-human-stop no-result branch and one direct focused test; DRM-03c2b owns the deferred normal, concession, and replacement coverage. The split preserves approved semantics and acceptance criteria.
- User and Orchestrator approved the 2026-08-05 DRM-04 split into DRM-04a,
  DRM-04b, and DRM-04c. DRM-04a is current for export-shape-only mapping and
  compile-required fixture literals; DRM-04b owns importer writes; DRM-04c owns
  the atomic version flip and explicit v1 rejection. The split preserves all
  approved semantics and acceptance criteria.
- User and Orchestrator approved the 2026-08-05 DRM-05a..DRM-05e serial split.
   DRM-05a owns competitive SQL semantics and focused DB coverage; DRM-05b
   owns the main-profile data boundary and removal of `?bots=1`; DRM-05c owns
   the main-profile-only selector; DRM-05d owns competitive presentation and
   markers; DRM-05e owns authoritative Game results UI. The existing history
   route remains unchanged and game-type/history rollout is #59.
- Orchestrator approved the 2026-08-05 administrative recovery split of
   reportless DRM-05b into serial DRM-05b1 query/DTO, DRM-05b2
   consumers/histograms, and DRM-05b3 SSR coverage. The split preserves the
   approved main-profile-only boundary, leaves the history route unchanged, and
   does not alter acceptance criteria.
- Orchestrator approved the 2026-08-05 DRM-05c narrowing: it owns only the
   authoritative Game results DTO/query and focused DB coverage in
   `rust/web/src/stats/queries.rs`. DRM-05d owns the deferred main-profile
   server/resource/UI boundary. This preserves approved result semantics and
   does not alter acceptance criteria.
- Orchestrator approved the 2026-08-05 DRM-05d1 -> DRM-05d2 -> DRM-05d3
  serial split and the main-profile URL/default decisions. DRM-05d1 owns only
  client DTO visibility and the server boundary; DRM-05d2 owns the approved
  URL/default and resource boundary; DRM-05d3 owns rendering and focused SSR
  coverage. The split preserves approved main-profile-only scope, authoritative
  Game results semantics, and hydration acceptance criteria.
- Orchestrator approved the 2026-08-05 DRM-05d1 correction: retain all existing
  d1 hunks and add only `viewer_user_id` and `can_add_friend` with the existing
  competitive profile-header semantics. This preserves approved scope and
  acceptance criteria.
